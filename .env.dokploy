# =============================================================================
# Environment Variables for Dokploy Deployment
# =============================================================================
# Copy these variables to Dokploy Dashboard > Project > Environment Variables
#
# Required variables are marked with [REQUIRED]
# Optional variables are marked with [OPTIONAL] - leave empty to disable feature
# =============================================================================

# -----------------------------------------------------------------------------
# [REQUIRED] PostgreSQL Database
# -----------------------------------------------------------------------------
# If using Dokploy's built-in Postgres, set these:
DB_USER=zenvoyer_db
DB_PASSWORD=your_secure_password_here
DB_NAME=zenvoyer_db

# If using external/shared Postgres, also set:
# DB_HOST=your-postgres-host.com
# DB_PORT=5432

# -----------------------------------------------------------------------------
# [OPTIONAL] WAHA WhatsApp Validation
# -----------------------------------------------------------------------------
# Enable real-time WhatsApp number validation via WAHA API
# Leave empty to disable WhatsApp validation (will skip this check)
WAHA_BASE_URL=
WAHA_API_KEY=
# WAHA_SESSION=default
# WAHA_TIMEOUT=10

# -----------------------------------------------------------------------------
# [OPTIONAL] Google Sheets Sync
# -----------------------------------------------------------------------------
# Enable automatic sync of results to Google Sheets
# Leave empty to disable (results will only be saved to local files/database)
SPREADSHEET_ID=

# For Google Sheets, you need service account credentials.
# Option 1: Mount credentials file and set path
# GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/service-account.json

# Option 2: Base64 encode the JSON and store as secret (recommended for Dokploy)
# GOOGLE_CREDENTIALS_BASE64=eyJ0eXBlIjoic2VydmljZV9hY2NvdW50Ii...

# =============================================================================
# Memory Requirements
# =============================================================================
# - postgres: 512MB
# - dashboard: 512MB
# - worker: 2GB (Playwright + Camoufox browsers)
#
# Total recommended: 4GB RAM minimum for the server
# =============================================================================
