# Core dependencies for Email Scraper & Validator
scrapling[all]>=0.2.0
email-validator>=2.0.0
py3-validate-email>=1.0.5
validate_email>=1.3
pandas>=2.0.0
tqdm>=4.65.0
beautifulsoup4>=4.12.0
phonenumbers>=8.13.0
chardet>=5.0.0
psutil>=5.9.0
requests>=2.31.0

# Database export dependencies (optional - only needed with --export-db flag)
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0

# Dashboard dependencies (FastAPI web dashboard)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
jinja2>=3.1.2

# Additional dependencies included with scrapling[all]:
# - playwright
# - beautifulsoup4
# - lxml
# - camoufox (stealth browser)
gspread>=5.12.0
google-auth>=2.30.0

# XLSX import support (toolkit/import_to_db.py)
openpyxl>=3.1.0

# File system monitoring (configuration hot-reload)
watchdog>=3.0.0

# YAML configuration parsing
pyyaml>=6.0.1

# Extended typing support (Literal, Final)
typing_extensions>=4.7.0
